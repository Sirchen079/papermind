# 版本变化

## 0.5.4 — 2026-09-24

- 修复 PDF 划词文本层与缩放后的页面错位，优化选区高亮、浮动工具栏定位、缩放定位、草稿保存与失败重试。
- 划词翻译使用独立可拖动浮窗，支持按项目配置专用翻译模型。
- 新增 PDF → Markdown：自动提取文本或全文 OCR，支持进度、停止、断点续跑、预览与图文包导出；保留原文和逐页原图。
- OCR 模型可独立配置，支持图片输入的多模态模型及兼容对话接口的专用 OCR 模型。
- 转换结果持久化供 AI 阅读并更新向量索引，完整备份与跨项目复制包含 Markdown 和原图。
- 新增专用模型与大模型两种重排序模式。大模型模式可与对话、翻译和 OCR 共用文本模型，向量模型仍需单独配置用于召回。
- 重排序校验候选编号，失败、超时或输出无效时保留原向量检索结果；两种模式分别保存模型选择。

## 0.5.3 — 2026-09-20（基础体验更新，替换同版本安装包）

### 阅读与科研讨论更新

- 阅读器支持划词中英翻译与侧栏 AI 伴读，携带选文及页码连续提问，保留历史与草稿。
- 打开阅读器或多篇讨论时后台准备全文，显示加载状态并复用缓存；失败可重试，不阻断已有材料的讨论。
- 论文库多选后可“基于所选论文讨论”，持续关联论文集合，支持刷新恢复、队列和失败重试，自动检索优先限定所选论文。
- 普通讨论默认关闭额外证据复核，复核异常不再丢弃已有回答；允许 idea 探索，并保留对研究背景与需求的主动澄清。


- 对话支持粘贴截图、拖放和上传图片／PDF／Word／文本，附件可预览、移除并保留在历史中。
- 生成期间可准备下条消息并顺序排队；停止、报错或请求澄清时暂停队列，支持编辑、移除和手动继续。
- 未发送附件与队列在本机持久化，刷新后恢复，按资料库、项目和对话隔离。
- 对话内可选择模型并配置思考等级、上下文窗口和图片能力；显式思考配置不再被静默忽略。
- 工具执行状态及历史可见，达到轮次上限可从已保存进度续接；显示上下文估算及压缩提示。
- 刷新后可停止后台回答，保留新输入的草稿，修复窄窗口输入框被按钮挤窄的问题。
- 仍保留以下桌面启动修复；旧版 0.5.3 用户需重新下载安装包。

### 2026-09-19 桌面启动修复

- 修复 0.5.3 桌面依赖未完整打包导致无法启动的问题。
- 构建前强制检查桌面依赖，打包后验证真实 WebView2 窗口、React 页面渲染及退出。
- 启动异常显示实际原因，不再将缺失依赖误报为未安装 WebView2。


- 采用 GPL-3.0 协议发布，并重写 README。
- 论文问答的工具调用步数上限默认提高到 100，可在设置页「论文问答」调整（4–200）。
- 模型支持按模型配置上下文窗口、思考等级（low/medium/high/xhigh/max）与图片输入能力标记；配置随共享连接同步。
- 刷新模型列表不再覆盖手工填写的上下文窗口。

## 0.5.2 — 2026-09-16

- 桌面启动等待 WebView2 初始页面加载完成后再打开研究空间，避免初始化与导航竞争。
- 检查前端是否已挂载；加载失败自动重试一次，持续失败通过原生对话框报告并退出。
- 增加启动阶段日志和慢初始化、白屏、导航失败、渲染器无响应、关闭取消回归测试。
- 修复数据库迁移覆盖桌面日志配置，以及窗口销毁后读取尺寸的异常。

## 0.5.1 — 2026-09-15

- 内置改编的 Nature Paper Card 证据技能与 K-Dense 批判性比较技能，附上游版本、许可证和修改说明。
- 论文问答和 Wiki 更新增加独立证据复核，保留来源快照与修订记录；区分材料未报告、研究未做、部署与因果收益。
- 修复 Wiki 引用编号和正文引用列表不一致，以及结构化输出被截断后的处理；调整小上下文模型的证据预算。
- 完善部分推理模型官方接口的适配，保留工具调用所需的推理状态，并为生成和证据复核设置明确的网络超时。
- 全文工具支持定位与分页，Agent 在结束前检查检索完整性；修复 PDF 导入阻塞、阅读页布局和恢复脚本依赖路径问题。

## 0.5.0

- 新增独立研究项目、项目内会话和资料管理，以及显式跨项目复制。
- 新增带版本和引用来源的专题知识，区分候选内容与已采用判断。
- 支持共享模型连接和项目内模型选择。
- 增加整应用备份、离线恢复和项目恢复检查。
- 修复并发导入、模型默认项竞争、密钥创建与恢复、草稿保存、会话切换、来源跳转和筛选结果覆盖问题。
